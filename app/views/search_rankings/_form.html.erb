<%= form_with(model: search_ranking) do |form| %>
  <% if search_ranking.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(search_ranking.errors.count, "error") %> prohibited this search_ranking from being saved:</h2>

      <ul>
        <% search_ranking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
